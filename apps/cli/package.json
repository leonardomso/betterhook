{
  "name": "@betterhook/cli",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cargo build -p betterhook-cli",
    "test": "cargo test -p betterhook-cli",
    "lint": "cargo clippy -p betterhook-cli -- -D warnings"
  }
}
